class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        maxi = 0
        n = len(prices)
        mini = float('inf')
        for num in prices:
            mini = min(mini,num)
            maxi = max(maxi,num - mini)
        return maxi



